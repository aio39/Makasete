import DarkModeToggle from './DarkModeToggle';

const Navigation = () => {
  return (
    <header className="flex flex-col ">
      <DarkModeToggle />
    </header>
  );
};

export default Navigation;
